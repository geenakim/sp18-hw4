  <%= form_for :tasks do |f| %>
  Task: <%= f.text_field :ftasks %><br />
  Finished? : <%= f.check_box :admin %><br />
  <%= f.submit %>

  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>

  <%= submit_tag("Create") %>

<% end %>

